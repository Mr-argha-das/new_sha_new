export const BASE_SPACING = 4;

export enum Sizes {
  MD = 16,
  MS = 14,
  SM = 12,
  XS = 8,
  XXS = 6,
  ML = 18,
  LG = 20,
  XL = 24, // @TODO : Make this value 22 if needed
  XXL = 32,
  XXXL = 40,
}

export enum FontSize {
  XS = 8,
  XSS = 10,
  SM = 12,
  BASE = 14,
  MD = 16,
  LG = 18,
  XL = 22,
  XXL = 28,
  XXXL = 36,
}

export enum BorderRadius {
  SM = 4,
  MD = 6,
  LG = 8,
  XL = 12,
  XXL = 16,
}

export const SIDEBAR_WIDTH = 256;

export const SIDEBAR_WIDTH_COLLAPSED = 60;

export const APPBAR_HEIGHT = 64;
