export enum Cookie {
  AccessToken = 'ibms.access_token',
  Tenant = 'ibms.tenant',
}

export enum FirebaseError {
  UserNotFound = 'auth/user-not-found',
  InvalidPassword = 'auth/wrong-password',
  InvalidCredential = 'auth/invalid-credential',
}

export enum Mode {
  VIEW = 'view',
  EDIT = 'edit',
  DELETE = 'delete',
}

export enum GeneralInfoMode {
  VIEW = 'view',
  EDIT = 'edit',
}

export enum LegalAndTaxInfoMode {
  VIEW = 'view',
  EDIT = 'edit',
}

export enum FinancialInfoMode {
  VIEW = 'view',
  EDIT = 'edit',
}

export enum DigitalProfileInfoMode {
  VIEW = 'view',
  EDIT = 'edit',
}

export enum AddressInfoMode {
  VIEW = 'view',
  EDIT = 'edit',
}

export enum CustomerRelationshipInfoMode {
  VIEW = 'view',
  EDIT = 'edit',
}

export enum UserInfoMode {
  VIEW = 'view',
  EDIT = 'edit',
}

export enum CommentInfoMode {
  VIEW = 'view',
  EDIT = 'edit',
}
